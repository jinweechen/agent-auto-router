[project]
name = "agent-auto-router"
version = "0.7.0+codex.20260811022119"
description = "Deterministic, registry-driven routing for Codex, Claude Code, and generic agent hosts"
requires-python = ">=3.10"
license = { file = "LICENSE" }
dependencies = []
