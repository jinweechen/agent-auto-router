[project]
name = "codex-auto-router"
version = "0.4.0"
description = "Deterministic, measurable, registry-driven routing for signed-in Codex CLI tasks"
requires-python = ">=3.10"
license = { file = "LICENSE" }
dependencies = []
