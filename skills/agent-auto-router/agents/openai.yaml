interface:
  display_name: "Agent Auto Router"
  short_description: "Route models and coordinate safe subagents"
  default_prompt: "Use $agent-auto-router to select trusted models and coordinate a bounded single-writer Desktop or CLI workflow."
